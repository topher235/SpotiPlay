<!DOCTYPE html>
{% extends 'play_app/base.html' %}
{% load staticfiles %}
	{% block title %}
		Search Results
	{% endblock %}

	{% block body_block %}
		<div class="row"><h1>Showing results for {{artistList.0.name}}</h1></div>
		<div class="row">
		<div class="col">
			<img src="{{ image_url }}" class="rounded img-fluid"/>
		</div>
		<div class="col">
			<div id="accordion" role="tablist" aria-multiselectable="true">
				{% for artist in artistList %}
					<div class="card">
						<!-- HEADER -->
						<div class="card-header" role="tab" id="{{artist.heading}}">
							<h5 class="mb-0">
								<a href="#{{artist.body}}" data-toggle="collapse" data-parent="#accordion" aria-expanded="false" aria-controls="{{artist.body}}">
									{{artist.name}}
								</a>
								<p>{{artist.setlist.venue_name}}</p>
								<p>{{artist.setlist.venue_city}}</p>
								<p>{{artist.setlist.date}}</p>
							</h5>
						</div>
						<!-- BODY -->
						<div id="{{artist.body}}" class="collapse" role="tabpanel" aria-labelledby="{{artist.heading}}">
							<div class="card-block">
								<h2> Songs </h2>
								<ol>
									{% for song in artist.setlist.set_list %}
										<li>{{ song.name }}</li>
									{% endfor %}
								</ol>
								<h2> Encore </h2>
								<ol>
									{% for song in artist.setlist.encore_list %}
										<li>{{ song.name }}</li>
									{% endfor %}
								</ol>
							</div>
						</div><!-- end body -->
					</div>
				{% endfor %}
			</div> <!-- end accordion -->
		</div> <!-- end col -->
		</div> <!-- end row -->
	{% endblock %}